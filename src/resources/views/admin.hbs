    <h1>ADD NEW CLOTHES</h1>
    <form action="/admin/addproducts" method="POST" class="container" enctype="multipart/form-data">

  
    <div>
        <div class="form-group">
        <label>Product ID</label>
        <input type="text" class="form-control" name="ProID" placeholder="Cloth name" value="{{data.ProID}}" >
        <div class="text-danger">
            </div>
    </div>

    <div>
        <div class="form-group">
        <label>Name</label>
        <input type="text" class="form-control" name="ProName" placeholder="Cloth name" value="{{data.ProName}}" >
        <div class="text-danger">
            </div>
    </div>

    <div class="form-group">
        <label>Price</label>
        <input type="text" class="form-control" name="Price" placeholder="price"  value="{{data.Price}}">
        <div class="text-danger">
           </div>
    </div>

    <div class="form-group">
            <label>Quantity</label>
            <input type="text" class="form-control" name="numofpro" placeholder="quantity" value="{{data.numofpro}}" >
    </div>
    
    <div class="form-group">
        <label>Description</label>
        <input type="text" class="form-control" name="Description" placeholder="bla bla bla" value="{{data.Description}}">
        <div class="text-danger">
            </div>
    </div>

    <div class="upimg">
        <label >Upload image</label>
        

    <div class="center">
        <div class="form-input">
          <label for="file-ip-1">Upload Image</label>
          <input type="file" name="imglink" id="file-ip-1" accept="image/*" onchange="showPreview(event);" required>
          <div class="preview">
            <img id="file-ip-1-preview">
          </div>
        </div>
      </div>       
    </div> 
      <button>ADD PRODUCT</button>
      <a class="btn btn-secondary" href="admin/viewproducts/"><i class="fa fa-list-alt"></i> View All</a>
    </form>
     <style>
          

.center {
  height:100%;
  display:flex;
  align-items:center;
  justify-content:center;
}
.form-input {
  width:500px;
  padding:20px;
  background:#fff;
  border:2px dashed #555;
}
.form-input input {
  display:none;
}
.form-input label {
  display:block;
  width:100%;
  height:50px;
  line-height:50px;
  text-align:center;
  background:#333;
  color:#fff;
  font-size:15px;
  font-family:"Open Sans",sans-serif;
  text-transform:Uppercase;
  font-weight:600;
  border-radius:10px;
  cursor:pointer;
}

.form-input img {
  width:100%;
  display:none;
  margin-top:10px;
}
      </style>
    <script>
        function showPreview(event){
  if(event.target.files.length > 0){
    var src = URL.createObjectURL(event.target.files[0]);
    var preview = document.getElementById("file-ip-1-preview");
    preview.src = src;
    preview.style.display = "block";
  }
}

    </script>
